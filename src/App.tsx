import React,{useEffect,useState}from'react';type R={id:number;book:string;he_book:string;category:string;ref:string;text:string;source:string};const cats=[['all','הכל'],['tanakh','תנ״ך'],['mishnah','משנה'],['tosefta','תוספתא'],['bavli','בבלי'],['yerushalmi','ירושלמי'],['rambam','רמב״ם'],['rishonim','ראשונים'],['tur','טור'],['shulchan_arukh','שולחן ערוך'],['midrash','מדרש'],['responsa','שו״ת'],['halakhah','הלכה'],['thought','מחשבה'],['private','פרטי']];function Settings({close}:{close:()=>void}){const[key,setKey]=useState(''),[model,setModel]=useState('gpt-5.6-luna'),[has,setHas]=useState(false),[msg,setMsg]=useState('');useEffect(()=>{window.hayad.getAI().then(x=>{setModel(x.model);setHas(x.hasKey)})},[]);async function save(){await window.hayad.saveAI({apiKey:key,model});setHas(true);setKey('');setMsg('נשמר מוצפן במחשב זה בלבד')}async function test(){setMsg('בודק...');const x=await window.hayad.testAI({apiKey:key});setMsg(x.ok?'החיבור תקין ✓':x.error)}return <div className="modal"><div className="settings"><button className="x" onClick={close}>×</button><h2>הגדרות AI למשתמש זה</h2><p>המפתח נשמר מוצפן ב-Windows ואינו חלק מה-EXE או מהעדכונים.</p><label>OpenAI API Key<input type="password" value={key} onChange={e=>setKey(e.target.value)} placeholder={has?'מפתח כבר שמור — הזן רק כדי להחליף':'sk-...'}/></label><label>מודל<select value={model} onChange={e=>setModel(e.target.value)}><option value="gpt-5.6-luna">GPT-5.6 Luna — חסכוני</option><option value="gpt-5.6-terra">GPT-5.6 Terra — מאוזן</option><option value="gpt-5.6-sol">GPT-5.6 Sol — חזק</option></select></label><div className="row"><button onClick={test}>בדוק חיבור</button><button className="gold" onClick={save}>שמור</button><button onClick={async()=>{await window.hayad.deleteAI();setHas(false);setMsg('המפתח נמחק')}}>מחק מפתח</button></div><b>{msg}</b></div></div>};function Library({back,settings}:{back:()=>void;settings:()=>void}){const[q,setQ]=useState(''),[cat,setCat]=useState('all'),[res,setRes]=useState<R[]>([]),[sel,setSel]=useState<R|null>(null),[ctx,setCtx]=useState<any>(null),[ms,setMs]=useState<number|null>(null),[stats,setStats]=useState<any>(null);useEffect(()=>{window.hayad.stats().then(setStats)},[]);async function go(){const x=await window.hayad.search(q,cat,80);setRes(x.results);setMs(x.elapsedMs);setSel(null)}async function pick(r:R){setSel(r);setCtx(await window.hayad.context(r.id,r.source,6))}async function add(){const x=await window.hayad.importSource();if(x?.ok){alert(`נוסף ${x.title}: ${x.count} קטעים`);setStats(await window.hayad.stats())}}return <div><header><button onClick={back}>← חזרה</button><div><strong>המאגר התורני המקומי</strong><small>{stats?`${stats.builtIn.books} ספרים · ${Number(stats.builtIn.segments).toLocaleString()} קטעים · OFFLINE`:'טוען...'}</small></div><span/><button onClick={add}>＋ ספר / OCR</button><button onClick={settings}>⚙ API</button></header><div className="search"><div className="line"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&go()} placeholder="איפה כתוב... / הדבק ציטוט"/><button className="gold" onClick={go}>חפש</button></div><div className="chips">{cats.map(c=><button className={cat===c[0]?'on':''} key={c[0]} onClick={()=>setCat(c[0])}>{c[1]}</button>)}</div>{ms!==null&&<small>נמצאו {res.length} תוצאות ב-{ms}ms — ללא אינטרנט</small>}</div><main><section className="results">{res.map((r,i)=><button className={'result '+(sel===r?'chosen':'')} key={i} onClick={()=>pick(r)}><b>{r.he_book||r.book}</b><em>{r.ref}</em><p>{r.text.slice(0,230)}</p></button>)}</section><section className="preview">{sel?<><div className="pvhead"><div><h2>{sel.he_book||sel.book}</h2><b>{sel.ref}</b></div><div><button onClick={()=>navigator.clipboard.writeText(sel.text)}>העתק</button><button className="gold" onClick={()=>navigator.clipboard.writeText(`${sel.text}\n(${sel.ref})`)}>העתק עם מקור</button></div></div><article>{ctx?.rows?.map((x:any)=><p className={x.id===ctx.selected.id?'focus':''} key={x.id}>{x.text}</p>)}</article></>:<div className="empty"><h2>תצוגה מקדימה</h2><p>בחר תוצאה. הספר ייפתח כאן בגלילה בלי לצאת ממסך העבודה.</p></div>}</section></main></div>}function App(){const[page,setPage]=useState<'home'|'library'|'work'>('home'),[settings,setSettings]=useState(false),[master,setMaster]=useState(''),[draft,setDraft]=useState('');useEffect(()=>{window.hayad.loadDraft().then(setDraft)},[]);async function load(){const x=await window.hayad.pickMaster();if(x?.ok){setMaster(x.text);setPage('work')}}return <>{settings&&<Settings close={()=>setSettings(false)}/>} {page==='home'?<div className="home"><h1>הי״ד החזקה - רבי משה בן מימון</h1><div className="homeBtns"><button onClick={load}>טען מאסטר</button><button className="gold" onClick={()=>setPage('library')}>מאגר תורני מקומי</button><button onClick={()=>setSettings(true)}>⚙ הגדרות API</button></div><div className="welcome"><h2>מכון הרמב״ם הבהיר</h2><p>עריכה, הגהה ומחקר מקומי. המאגר עובד גם ללא אינטרנט.</p></div></div>:page==='library'?<Library back={()=>setPage('home')} settings={()=>setSettings(true)}/>:<div><header><button onClick={()=>setPage('home')}>⌂ שער</button><strong>סביבת הגהה</strong><span/><button className="gold" onClick={()=>setPage('library')}>מאגר מקומי</button><button onClick={()=>setSettings(true)}>⚙ API</button></header><div className="work"><section><h2>לשון הרמב״ם</h2><article className="master">{master}</article></section><section><h2>— הרמב״ם הבהיר —</h2><textarea value={draft} onChange={e=>setDraft(e.target.value)} placeholder="כתוב כאן את ההגה..."/><button className="gold" onClick={()=>window.hayad.saveDraft(draft)}>שמור</button></section></div></div>}</>};export default App;